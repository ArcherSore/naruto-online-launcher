# AGENTS.md

## 项目定位

本项目以 `Chrispsz/naruto-online-launcher` 为技术底座，重构为腾讯国服《火影忍者 OL》专用启动器。启动器通过腾讯官方网页完成扫码登录、选服并进入 Flash 游戏，并继续承载多账号、后台运行和自动化能力。不要求兼容 Oasis/巴西服；原有 Oasis 专用逻辑可按国服架构删除、替换或重构。

## 沟通与事实依据

- 文档、分析、计划和回答使用中文；代码标识符、命令和原始错误信息保持原文。
- 优先通过 `docs/REPO_MAP.md` 定位代码，但修改前必须阅读真实源码。
- 信息优先级依次为：当前源码与配置、实际运行和测试结果、`docs/REPO_MAP.md`、历史文档。
- `MIGRATION_PROMPT.md` 仅是上游历史交接资料，不得视为当前需求或事实来源。

## 技术与安全边界

- 固定使用 Volta 配置的 Node.js 16.20.2、npm 8.19.4 和 Electron 11.5.0。
- 保留 PPAPI Flash 能力。未经明确要求，不升级 Node.js、npm、Electron 或核心依赖，不无故修改 `package-lock.json`。
- 国服登录必须使用腾讯官方网页扫码流程；不得采集 QQ 密码，不得伪造、解密、重放或绕过登录票据、验证码及风控。
- 不同账号必须使用相互隔离的 Electron Session/Partition。

## 修改原则

- 原 Oasis 登录、区域、服务器、页面注入和网络规则可删除或替换，不为向后兼容保留无用逻辑。
- 修改围绕当前任务，避免无关的大规模格式化、重命名或重构。
- 新增代码应保持登录、Session、窗口、Flash 和自动化模块职责清晰。
- 通用自动化能力只以 Profile、窗口、webContents、内容尺寸、输入和动作生命周期等客观资源
  有效性作为框架门槛；页面阶段、`GAME_READY` 或未来图像识别条件属于诊断/脚本策略，不得在
  无独立安全必要性时全局禁用截图、录点或脚本启动。

## 验证与交付

- 修改后运行相关测试。涉及扫码、网页跳转或 Flash 且无法自动测试时，必须给出明确的人工验证步骤。
- 最终回复必须列出修改文件、验证命令、验证结果和尚未验证的风险。
